# 复制为 .env.local 并填入真实值。.env.local 已在 .gitignore 里,不会被提交。
#
# P0(纯 mock)不需要任何环境变量,留空也能正常玩。
# P1 才用得到下面这个:配置了就自动为"填词区"场景(玩家输入没命中
# 具体内容时)接入真实叙述;没配置就自动回落到模板池,不会报错。

# DeepSeek API key(https://platform.deepseek.com/ 申请)
DEEPSEEK_API_KEY=
